Per-photo sync/storage glyph. States: `local` (drive, gray), `synced` (green cloud-check), `syncing` (amber spinner), `offloaded` (amber cloud — original lives only in pCloud), `error` (red).

```jsx
<StatusGlyph state="offloaded" />
```
