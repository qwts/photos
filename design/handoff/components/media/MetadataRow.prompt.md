Inspector key/value row. Labels are uppercase mono; values are mono for machine data (default), sans for human text (`mono={false}` for filenames, album names).

```jsx
<MetadataRow label="Camera" value="FUJIFILM X-T5" />
<MetadataRow label="Backup" value="Encrypted · pCloud" tone="var(--accent-green)" />
```
