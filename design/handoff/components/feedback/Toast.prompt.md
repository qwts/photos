Transient notification. Tones follow status hues (green = backup/encryption success, amber = sync activity, red = failure).

```jsx
<Toast tone="green" title="Backup complete" detail="1,204 photos encrypted → pCloud" />
```
