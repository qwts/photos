Thin 4px progress bar. Detail text is always mono (`"842 / 1,204"`). Tone: cyan = import/thumbnails, amber = cloud transfer, green = encryption.

```jsx
<ProgressBar label="Generating thumbnails" detail="842 / 1,204" value={842} max={1204} />
```
