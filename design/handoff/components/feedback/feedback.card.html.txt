<!-- @dsCard group="Components" viewport="700x400" subtitle="Dialog, Toast, ProgressBar" name="Feedback" -->
<!DOCTYPE html><html><head>
<link rel="stylesheet" href="../../styles.css">
<script src="https://unpkg.com/lucide@0.462.0/dist/umd/lucide.min.js"></script>
<script src="https://unpkg.com/react@18.3.1/umd/react.production.min.js"></script>
<script src="https://unpkg.com/react-dom@18.3.1/umd/react-dom.production.min.js"></script>
<script src="../../_ds_bundle.js"></script>
<script src="https://unpkg.com/@babel/standalone@7.26.4/babel.min.js"></script>
<script src="../../ds-loader.js" data-root="../.."></script>
</head>
<body style="margin:0;background:var(--gray-0);font-family:var(--font-ui);">
<div id="root"></div>
<script type="text/babel">
const DS = window.DS || {};
const { Toast, ProgressBar, Dialog, Button, Checkbox } = DS;
function Card() {
  return (
    <div style={{ display: "flex", gap: 16, padding: 16 }}>
      <div style={{ flex: 1, display: "flex", flexDirection: "column", gap: 12 }}>
        <Toast tone="green" title="Backup complete" detail="1,204 photos encrypted → pCloud" />
        <Toast tone="amber" title="Uploading to pCloud" detail="Encrypting 842 of 1,204…" />
        <Toast tone="red" title="Sync failed" detail="pCloud unreachable — retrying in 30s" />
        <div style={{ background: "var(--gray-1)", border: "1px solid var(--border-1)", borderRadius: "var(--radius-2)", padding: 14, display: "flex", flexDirection: "column", gap: 14 }}>
          <ProgressBar label="Generating thumbnails" detail="842 / 1,204" value={842} max={1204} />
          <ProgressBar label="Uploading (encrypted)" detail="3.2 GB / 8.9 GB" value={36} tone="amber" />
          <ProgressBar label="Re-encrypting library" detail="61%" value={61} tone="green" />
        </div>
      </div>
      <div style={{ flex: 1, position: "relative", minHeight: 340, background: "var(--gray-0)", borderRadius: "var(--radius-2)", overflow: "hidden" }}>
        <Dialog title="Delete 12 photos?" icon="trash-2" width={330}
          footer={<React.Fragment><Button variant="ghost">Cancel</Button><Button variant="danger">Delete</Button></React.Fragment>}>
          <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
            <span>Photos are removed from the encrypted library and every album.</span>
            <Checkbox label="Also delete pCloud backup copies" />
          </div>
        </Dialog>
      </div>
    </div>
  );
}
ReactDOM.createRoot(document.getElementById("root")).render(<Card />);
</script>
</body></html>
