Square icon-only button — the workhorse of Overlook toolbars. `active` gives a cyan-tinted toggled state (e.g. current view mode, open inspector).

```jsx
<IconButton icon="info" title="Inspector" active={inspectorOpen} onClick={toggle} />
<IconButton icon="trash-2" title="Delete" />
```
