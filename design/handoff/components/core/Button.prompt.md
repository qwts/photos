Standard push button. One `primary` per surface; `secondary` is the default; `ghost` for toolbars; `danger` for destructive confirms.

```jsx
<Button variant="primary" icon="download">Import 1,204 photos</Button>
<Button variant="ghost" icon="share">Export</Button>
<Button variant="danger">Delete 12 photos</Button>
```
