Segmented control for exclusive choices — view mode, sort order.

```jsx
<Segmented value={view} onChange={setView}
  options={[{ value: "grid", icon: "layout-grid", iconOnly: true, label: "Grid" },
            { value: "list", icon: "list", iconOnly: true, label: "List" }]} />
```
