Library search input, lives in the toolbar. Placeholder copy hints at capability ("Search photos, places, cameras…") — later semantic search reuses this field unchanged.

```jsx
<SearchField value={q} onChange={setQ} />
```
