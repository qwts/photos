Filter chip for the filter bar. Selected = cyan outline + tint. Active applied filters get `onRemove`.

```jsx
<Chip icon="image" selected>RAW</Chip>
<Chip icon="cloud" onRemove={clear}>Offloaded</Chip>
```
