---
---

Adopt the shared documentation-governance gate (docs-gov, ENG-0009): add
`docs-gov.config.json` and a `Docs governance` CI job consuming
`qwts/playbook-engineering/.github/workflows/docs-governance.yml@v1`. Tooling/docs
only — no shipped behavior changes, so no release.
