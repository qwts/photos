---
'photos': minor
---

Migrate signed macOS builds to `com.zts1.overlook` while preserving the existing Overlook profile, app-lock anchor, library, provider custody, and recovery data. Existing Touch ID enrollments from the former app identity now require an explicit password-confirmed re-enrollment.
