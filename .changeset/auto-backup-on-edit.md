---
'photos': patch
---

Edits now back up themselves: favoriting, album changes, and restores trigger the same quiet auto-backup imports use (when "Back up new imports automatically" is on), so the "ENCRYPTING → PCLOUD" indicator no longer sits until a manual backup.
