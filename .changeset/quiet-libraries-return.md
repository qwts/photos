---
'photos': patch
---

Reuse retained legacy `photos` profiles after reinstall so local libraries and provider connections remain available, let fresh-profile onboarding open an existing library without downloading a cloud backup, and preserve the previous library if failed restore activation recreates a partial target directory.
