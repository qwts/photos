---
'photos': patch
---

macOS builds are now signed with a Developer ID certificate and notarized by Apple; tagged releases publish as full releases instead of unsigned pre-releases (#128). Windows builds remain unsigned pending a certificate.
