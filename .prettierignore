node_modules/
dist/
out/
release/
storybook-static/
.test-dist/
.test-dist-dom/
coverage/
.claude/
playwright-report/
test-results/
package-lock.json
design/handoff/
# Generated verbatim by scripts/generate-third-party-notices.mjs; the
# licenses:notices freshness check compares raw generator output byte-for-byte,
# so prettier must not reformat it (#461).
THIRD-PARTY-NOTICES.md
# Generated verbatim by scripts/i18n-catalog.mjs; the i18n:check gate compares
# raw compiler output, so prettier must not reformat it (#403). Its JSON.stringify
# output uses double quotes, which prettier would rewrite to single.
src/shared/i18n/generated/
src/main/i18n/generated/
